# Core dependencies
requests>=2.31.0
pytest>=7.4.0
pytest-cov>=4.1.0
python-dotenv>=1.0.0

# Reporting dependencies
openpyxl>=3.1.0
reportlab>=4.0.0

# Model inference
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.25.0

# LLM evaluation (optional — only needed for --llm-evaluate)
# Note: airllm requires optimum<2.0 (bettertransformer) and sentencepiece.
# The airllm_base.py BetterTransformer import is patched to work with newer
# transformers versions (>=4.49) — see docs for manual patch instructions.
airllm>=0.9.0
optimum>=1.20.0,<2.0.0
sentencepiece>=0.1.99

# OpenAI API backend (optional — only needed for --llm-backend openai)
openai>=1.0.0
